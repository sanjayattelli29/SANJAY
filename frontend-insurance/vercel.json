{
  "rootDirectory": "frontend-insurance",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist/frontend-insurance",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}